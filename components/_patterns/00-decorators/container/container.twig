{% set _classes = [
	"",
] | merge(classes | default([]) )| join(" ")  %}

<div class="container {{ _classes }}">
	<div class="row">
		<div class="col-12">
			{%- block content %}
				{{ content }}
			{% endblock -%}
		</div>
	</div>
</div>
